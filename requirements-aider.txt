# Optional: Aider support for Claude/OpenAI/Ollama worker modes
#
# ⚠️  DEPRECATED — the Aider worker adapter (`workers/aider_adapter.py`) is
# deprecated as of the 0.9.0 release and will be removed in the next minor
# release. The three supported worker paths are now:
#   - opencode (default: opencode/deepseek-v4-flash-free, proven free path)
#   - openrouter_direct
#   - codex
#
# New solver runs should NOT install this file. If you have a legacy
# workflow still depending on Aider, install only as a stop-gap:
#
# Installieren (NOT recommended for new setups):
#   pip install -r requirements-aider.txt
#
# Migration notes: see docs/SETUP_AIDER.md banner and issue #411 / §47 in
# docs/BACKLOG/open.md.

aider-chat>=0.50.0